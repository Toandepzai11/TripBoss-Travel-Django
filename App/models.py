# models.py
# this is a models use for featured places in the home page
# from django.db import models

# class FeaturedPlace(models.Model):
#     name = models.CharField(max_length=255)
#     description = models.TextField(blank=True)  # Cho phép mô tả trống
#     image = models.ImageField(upload_to='featured_places/')
    
#     def __str__(self):
#         return self.name
